#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--)
	{
	    int a[3];
	    int c=0,d=0;
	    for(int i=0;i<3;i++)
	    {
	        cin>>a[i];
	        if(a[i]==0)
	        {
	            c++;
	        }
	        else if(a[i]==1)
	        {
	            d++;
	        }
	    }
	    if(c>=2)
	    {
	        cout<<"Water filling time"<<endl;
	    }
	    else
	    {
	        cout<<"Not now"<<endl;
	    }
	}
	return 0;
}
