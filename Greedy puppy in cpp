#include <iostream>
using namespace std;

int main() {
	int t,n,k,mx=0;
    cin >> t;

    while(t--)
    {
        cin >> n >> k;
        mx=0;

        for(int i=1 ; i<=k ; i++)
            mx = max(mx, n%i);

        cout << mx << endl;
    }
	
	return 0;
}
