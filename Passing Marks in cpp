#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	int amin,bmin,cmin,tmin,a,b,c;
	while(t--){
	    cin>>amin>>bmin>>cmin>>tmin>>a>>b>>c;
	    if(amin<=a && bmin<=b && cmin<=c && (a+b+c>=tmin)){
	        cout<<"yes"<<endl;
	    }
	    else{
	        cout<<"no"<<endl;
	    }
	}
	return 0;
}
